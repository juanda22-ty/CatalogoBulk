{
  "name": "catalogobulk",
  "version": "1.0.0",
  "description": "CatalogoBulk - frontend estático y API serverless para Vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ioredis": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
